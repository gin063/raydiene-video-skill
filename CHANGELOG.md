# CHANGELOG

倒序，最新在最上面。记的是 git 看不出来的东西：口径为什么这么定、哪些结论已验证哪些没有。

---

## 2026-08-28（十）· 图库定型复核

操作者完成了背景统一、RGB 转换、压缩、星耀目录替换。逐项复核结果：

| 项 | 结果 |
|---|---|
| 背景统一到纯白 255 | ✅ 28 张全部 255.0 |
| 体积 | ✅ 122 MB → **23.9 MB** |
| 星耀3/4 归档存疑 | ✅ 已替换，整个星耀目录换成 4 张新图 |
| 跨型号重复文件 `星耀4 == 星辰4` | ✅ 已消除 |
| RGBA 转 RGB | ⚠️ **`坚石2.png` 仍是 RGBA**（无透明），图库里唯一一张 |
| `磐石max5/6 == 磐石pro5/6` | 保留 —— 侧视看不出型号差异，属合理复用，非错误 |

**星耀 / 星辰的真实区别（操作者澄清 + 看图确认）**：
**机身完全相同，只差液晶面板尺寸**——星耀屏约占面板宽 60%，星辰约 35%。
我上一轮把它归因为「屏幕 UI 版本不同」，那是**症状不是原因**：屏大了才装得下
「30A 大数字 + 停止充电 + 四个底部图标」的布局，屏小只能放「六宫格 + 服务电话」。
索引里已改成按尺寸描述，UI 差异作为附带的识别特征保留。

**星耀新四张构成完整 2×2 组合**（正面/斜角 × 枪已插/枪座空），星辰缺「斜角+枪已插」那一格。

**Seedance 文档来源澄清**：官方文档就是操作者给的那两个文件，不存在"未读到"的部分。
中文 txt（216KB）是完整正文；**英文 mhtml 只有 19KB，是懒加载的目录壳**，正文没落盘，
只用于查官方英文术语（白模 = Clay Renderer）。`model-routing.md` 第 5 节已据此改写。

**Kling IMAGE 系列指南从待办里移除** —— 操作者不用 Kling 生成图像，图像走 ChatGPT / nano banana。

**图库现存缺口**（`product_images/_index.md` 第五节）：
26 张**俯仰角全是 0° 平视**（最大缺口）、无背面、无局部特写、无右侧 90°、无线缆收纳态、
星耀/星辰无 90° 侧视。本次镜头表里 A07 枪头特写、A09 的 +30° 机位、A10 近景都属外推。

---

## 2026-08-28（九）· 图库入库、画面内文字全部外移

**① 建立标准图库 `product_images/`**（29 个文件 / 26 张唯一图 / 5 个型号），
逐张看过并写成 `product_images/_index.md`：方位角（数字，便于算包夹）、俯仰、景别、
枪头状态、线缆状态、备注。头部记文件总数与更新日期，**用前先对文件数**。

**逐张看图查出四件事，都是文字索引查不出来的**：

- 🔴 **磐石pro 与 max 的真正区别是面板**：pro 是**青色点阵条形灯**，max 是**方形显示屏**。
  正面渲染图 98.6% 像素相同，挂错后出图看起来完全合理，人工几乎不可能发现。
  **这同时回答了挂了很久的「已解锁 显示在哪」**——磐石pro 根本没有文字屏。
- 🔴 **星耀3、星耀4 疑似归档错误**：它们的屏幕 UI 是「六宫格 + 服务电话」版（与星辰一致），
  而星耀1/2/5 是「30A 大数字」版；且 `星耀4.png` 与 `星辰4.png` 是同一个文件。待人工确认。
- **磐石pro5/6 == 磐石max5/6**：侧视图看不出型号差异，属合理复用，已在索引标明。
- **后壳是黑色的**，只有正面外框/面板是银色。侧视图的前银后黑分色，之前的 prompt 完全没写。

**图库规格结论（实测，推翻了我上一轮的建议）**：
跑了 17 个候选底色，**纯白 `#FFFFFF` 最优**（平均边缘对比度 110.7，最差糊边 11.0%），
中灰区间反而最差（对比度 60–69，糊边 16–20%）。原因是这几款产品的**轮廓边缘整体偏暗**
（边缘亮度中位数 坚石 156 / 磐石 99 / 星耀 89），中灰正好压在分布中间。
纯黑分离度也高（6.9%）但会让模型把画面读成暗环境，不能用。
→ **保持白底，并把星耀2/4、星辰1/4 的 253 统一到纯正 255。**
坚石通体浅银，约 9% 轮廓仍会与白底融合（顶部弧面），这个改底色解决不了，
已在索引标注：**以坚石为主体的镜头要在 prompt 里显式描述胶囊形轮廓**。

**② 画面内文字全部不再由图像模型生成**（操作者要求，后期在剪映加）。
`image-prompt.md` 第 ⑫ 要素从「用「」逐字写全」整条翻转成「不生成，改为留白」：

| 要加什么字 | prompt 里怎么写 |
|---|---|
| 画面下方的说明 | 「下方留出约四分之一画面高度的干净负空间」 |
| 数据面板里的读数 | 「面板内部没有任何字符，只是干净平整的发光底面」 |
| 图标下方的标签 | 「图标下方保留一片干净的空白区域」 |

🔴 **关键是留白要写进构图那一项**，否则模型把画面填满，后期没地方放字。
唯一例外是物件自身带的标识（配电箱警示标、车库层号），但**只画图形不写文字**——
小尺寸中文渲染出来必然是乱码。

**③ 交付结构改造**：每条 prompt 的代码块**下方**（不进代码块）加两行提示——
「参考图」提醒操作者别忘了挂图并指明挂哪几张，「后期加字」写明加什么、加在哪。
文件名只写代码块外：**写进正文会被模型当成画面内容**。

**镜头表改动**：A03/A04/A05/A10/B01 的画面内文字全部移除，改为留白 + 代码块外标注；
A10 的「隐藏式显示区」设定取消（磐石pro 没有屏），改为纯指示灯点亮；
全部 21 段 prompt 下方补了参考图与加字提示。

**顺带记两个选图缺口**（已写进索引第五节）：图库**全部只有平视 0° 俯仰、只有左侧 90°、
没有局部特写、没有线缆收纳态**。A07 的枪头特写、A09 的 +30° 机位、A10 的近景都属外推，
产品比例要重点核。

---

## 2026-08-28（八）· Codex 侧自主选图，与参考图库规格

**新增 `codex-execution.md` 第 3 节「参考图库与选图」**，这是 Codex 侧相对 Claude 侧的核心增量：

**Claude 读不到本地文件，Codex 读得到。** 所以 prompt 里永远不指定用哪张参考图，
但**选图这件事在 Codex 侧是 Codex 自己的活，不是操作者的活**——原来写的
「挂哪张由操作者指定，未指定就问」在 Codex 侧是浪费，已改。

**最容易被浪费的能力：同时挂多张，合成图库里没有的角度。**
图库不可能穷举所有机位，但不必只挑最接近的一张——需要一个没有的角度时，
挂**包夹住它的两到三张**让模型自己推（目标 30° 就用「正面 + 45°」）。
两条边界：包夹要真的包夹（目标落在区间外就成了外推，模型会瞎猜）；
2–3 张为宜，角度差异过大的图放一起会互相打架，超过 4 张收益转负。

**反向约束沿用 A02–A05 的教训**：没写产品参考句的镜头（内部结构镜）**不要硬挂产品图**。
产品渲染图里没有内部结构，硬挂等于让模型参考一张不存在的图。

**图库规格（操作者问的两个问题，答案与理由）**：

- **背景用实底，不要透明 PNG。** alpha 在多数生图管线里会被拍平，而且**常拍平成黑色**，
  模型会读成暗环境、把银灰外框压暗。风险不可控、收益为零。
- **底色选浅中性灰 #C0C0C0 上下，不是纯白。** 磐石Pro 是银灰金属外框，
  **纯白底上银色边缘会和背景糊在一起**，模型抠不清轮廓和比例——而比例正是参考图最该锁的东西。
- **格式 PNG 无损，不用 JPEG。** 这个产品的画面特征是「高对比硬边界 + 大面积平坦色块」，
  正好是 JPEG 振铃与块效应的重灾区，**压缩伪影会被模型当成材质纹理学进去**；
  logo 和面板小图标最先糊。必须用 JPEG 则质量 95+ 且 4:4:4。
- 长边 1500–2000px；同一套图必须同光照、同材质、同比例（多图参考时不一致会互相打架）。
- 命名要角度可读，图库里放 `_index.md` 写明每张的角度/焦段/线缆状态——
  文件名表达不了「这张是收纳态、那张是垂放态」。

⚠️ 另记一条：**不要用带环境的实拍图作为唯一的产品参考**，环境光会整个被带进生成结果。
实拍图适合作为**场景**参考另行挂载，与产品外观参考分开。

---

## 2026-08-28（七）· 「断头」检查，与两类校验标准的分野

**① 操作者在成图上看出一个我没看出的结构错：黑色充电线缆的另一头不知去向。**

我上一版只写了「线缆盘绕成圈挂在桩体上」——**这句话把线缆的两端都糊过去了**：
没交代它从哪儿出来，也没交代盘完之后末端在哪。读者一眼未必说得出哪里怪，
但大脑会去找那根线的尽头，找不到就觉得假。

正确拓扑（操作者给的，已写进 `install-scene.md` 第 1 节，含 ASCII 示意图）：

**桩体底部有两根线，方向相反，不能混。**
- **进线在底部左侧** —— 电源线，被白色 PVC 管包覆，来自左侧配电箱，沿墙横向右行后 90° 折上
- **出线在底部右侧** —— 黑色充电线缆，向下盘绕在桩体正下方墙面的 **U 形金属挂钩**上，
  末端枪头插回正面圆形枪座

配电箱也从右侧挪到**左侧**：进线接底部左侧，箱子放右侧就得从桩下交叉走管，
不合施工习惯也难看。左侧是最短路径。

新增 `install-scene.md` 第 2 节「**断头检查**」，写成通用规则：
**画面里每一根线、每一根管，两端都必须有明确去处。** 一端延伸出画外可以，
但要有明确走向，不能画到一半没了。同理每个物件都要能回答「它连着什么、靠什么固定」——
悬空的挂钩、没有上游的线管、没有进线的配电箱，都是同一类错。

**② 校验标准分两类，且方向相反。** 操作者明确要求，已写进 `codex-execution.md` 第 1.5 节、
`SKILL.md` 铁律 5、`physical-realism.md` 开头。

| | 不校验，不为它重出 | 必须校验，错了只能重出 |
|---|---|---|
| 内容 | logo 准确性、品牌字样、细微材质瑕疵、轻微色偏 | **场景与物体的结构完整性和逻辑性** |
| 理由 | Photoshop 可修，重出成本高得多 | 改不动，构图与物件关系生成时就定死了 |

口诀：**「PS 十分钟能修好的不重出，PS 修不了的必须重出。」**
而且结构错要**先改 prompt 再重出**——同一段 prompt 大概率会再错一次。

`codex-execution.md` 第 1 节标题从「不做自审核」改成「**不做美学自审**」，
因为原来那句话说过头了：不做的是好不好看的自我评价，**结构和逻辑的硬错必须自己看出来并报告**。

**③ 这两轮连起来看的规律**：
前几轮的错都在**表达**（尾帧增量、运镜、正文纯净度），这两轮的错都在**事实与结构**
（安装场景凭想象、线缆断头）。表达的错自己能查出来，事实与结构的错**只有见过真东西的人能查出来**。
所以现在的做法是：涉及真实物件时，先要资料、要实拍图，不要猜着写；
拿不到就明确标注为假设，而不是写得像确定的。

**镜头表改动**：A01/A08/A09/A10 的配电箱移到左侧、走线改为「配电箱→右行→折上→底部左侧」、
黑缆改为「底部右侧引出→下行→盘在 U 形挂钩上→枪头归位」；A01 首帧毛坯态相应地点出三处空位
（桩位、左侧配电箱位、正下方挂钩位）；A01 视频的施工顺序加入挂钩安装与两根线分别就位。

---

## 2026-08-28（六）· 拿到真实安装实拍图，安装场景全部推倒重写

操作者提供三张真实安装实拍（地库毛坯墙 / 黄白涂装墙 / 蓝白涂装柱面）。
**第一版凭想象写的安装场景有四处硬伤，全部是"想当然"而不是"不知道"。**
新增 `references/install-scene.md` 专门存这套构成逻辑。

| # | 我写的 | 实际 | 后果 |
|---|---|---|---|
| 1 | 「极简方正的壁挂式充电桩」 | **竖长圆角胶囊形**，银灰金属外框包黑色玻璃面板 | 文字与参考图打架，模型两边都不听，出四不像 |
| 2 | **没有配电箱** | 必有独立防水配电箱 + 黄色「当心触电」三角标 | 电从哪来毫无交代，一眼假。这条最致命 |
| 3 | 线管从桩体上方向上延伸 | 顶部下行 → 配电箱 → 横向 → **桩体底部**进线 | 走线逻辑整个反了 |
| 4 | 线缆挂枪座、垂弧近地 | 收纳态是**盘绕成 4–6 圈挂在桩体上、枪头插回正面下部圆形插座** | 不像成品 |
| 5 | 完工墙面整面浅灰白 | 白墙 + **横向彩色色带**（黄/蓝）+ 白色分隔带 | 太素，不像地库 |
| 6 | 毛坯墙写「模板拼缝、麻面」 | 更标志性的是**规则阵列的圆形穿墙螺栓孔** | 特征不够准 |
| 7 | 高度 1.3m（枪座中心） | **1.4–1.5m**（面板中心） | 偏低 |

**这次的根本教训，比这七条本身重要**：

前几轮我一直在打磨 prompt 的**表达**（十三要素、术语密度、首尾帧一致性），
但产出物错在**事实**上——我没见过真实安装场景，就按"一台设备挂在墙上"的通用想象写了。
表达再精确，描述的东西不存在也没用。

所以 `install-scene.md` 第 4 节写了一条通用规则：
**prompt 里的形态与场景描述必须与所挂参考图一致；不确定长什么样，先问，不要猜着写。**
`SKILL.md` 的 references 索引里把它标成「含真实安装环境的镜头必读」。

**顺带修的**：
- `physical-realism.md` 第 2 节加了「必须有配电箱」「进线从底部」「线管只走直角」三条穿帮点，
  并指向 `install-scene.md`。
- `codex-execution.md` 明确 **不要为 logo 不准而重出** —— 操作者用 Photoshop 修，
  重出一张的成本远高于修一个 logo。值得重出的是改不动的东西：构图、场景逻辑、参数数字。

**新发现的一个真问题（已列进镜头表待拍板）**：
三张实拍图上，面板只有一道横向条形指示灯和一个刷卡图标，**看不到文字显示屏**。
而 A10 要显示「已解锁」三个字。现在按「面板中部黑色玻璃区域是隐藏式显示区」写，
但如果产品没有这个能力，这一镜就是在演一个不存在的功能。等操作者确认。

**镜头表改动**：A01/A08/A09/A10 的安装场景全部重写；A06/B01 的产品形态描述纠正；
A10/B01 的「屏幕」改为「面板上部条形指示灯 + 中部显示区」。A02–A05 是内部结构，不受影响。

---

## 2026-08-28（五）· 实跑修正：交付格式与固定机位

**① 尾帧 prompt 必须全量交付，不许写增量。**
第一版镜头表的尾帧写成了「复制首帧，把第 N 段替换为……」，看着省 token，
实际把拼接重组的成本转嫁给了操作者，且每次人工拼接都多一次漏改错位的机会。操作者直接指出了。

把两件事分清楚写进了 `frame-strategy.md` 第 4 节：
**「只改几句」是写作纪律（保证不漂移），全量是交付格式。** `SKILL.md` 的交付段也加了一条。

附带发现：**尾帧真正要改的常常不止「主体状态」那一段。** 首帧无文字、尾帧有文字时，
禁止项里「除 X 外不要出现任何文字」那句要跟着改；对焦点、光线、构图重心也可能因变化量而必须微调
（A03 的焦平面从「触点间隙」变成「触点接触面」，A10 的布光多了一项自发光源）。
全量写出来这些自然对上，增量写法极易漏。

**② 固定机位镜头要显式禁止运镜。**
A01 改成了同机位倍速变身（毛坯车位 → 完工，参考 `docs/A1.png` 那组毛坯阳台变精装的截图）。
这类镜头的力量全部来自「同一个取景框里世界变了」，机位一漂对比就散。
而首尾帧构图完全一致时，视频模型**特别容易自己加推拉**。

所以视频 prompt 里要写死「机位绝对固定，禁止任何推拉摇移升降或手持晃动」。
这**不违反**「首尾帧镜头不写运镜」——那条禁的是运镜指令，这条是运镜约束，方向相反。
`video-prompt.md` 第 3 节 ④ 待补这个区分。

**③ 倍速变身镜头是「一个变化量」规则的正当例外。**
墙面、地面、照明、桩体四样同时变，但属于同一次施工的因果链，
而且「很多事同时发生」正是这类镜头的意义。已在镜头表里写明理由，防止后人照规则拆镜。

**④ 画风轨道的第一依据是需求方原话。**
我把「动画演示桩体被雨淋」的 A08 归成了写实摄影轨道，是没读需求方那四个字。已改回 3D 轨道。

**待回写进 references（累计）**：②的运镜区分、④的轨道判断依据、
「内部结构镜没有资料来源闸门」（A02–A05 全靠通用工业常识，是全片最高风险）。
①已回写完成。

---

## 2026-08-28（四）· 首次实跑：磐石Pro 产品片

产出 `E:\Raydieneppideo-output\shotlist.md` —— 11 镜、21 张图的 prompt。
**这是本 skill 第一次真正跑一遍，以下是跑的过程中发现的、文档里原来没写清楚的东西。**

**跑通了的**
- 需求确认单 → 拆镜 → 帧策略 → 图像 prompt → 视频 prompt 的顺序没有卡壳。
- 「一个镜头只能有一个变化量」在实战里立刻起作用：需求方一句「桩体被雨淋被灰尘吹」直接拆出三镜。
- 十三要素写出来的首帧 prompt 长度在 300-400 字，视频 prompt 在 100-150 字，
  **比例大致是 3:1**，符合「视频 prompt 应当明显更短」的预期。

**跑的时候才发现的问题，已经影响了这次的产出，但还没回写进 references**

1. **尾帧的「只改变化的那几句」在实际写作里不好交付。** 因为首帧 prompt 是连续散文分段，
   「哪一段」需要额外说明。这次的处理是在每个尾帧块开头写一行「复制首帧，仅替换第 N 段」，
   并把替换后的整段贴出来。**建议把这个做法写进 `frame-strategy.md` 第 4 节**——
   下次不写清楚替换范围，操作者会不知道该动哪里。

2. **禁止项里的「除 X 外不要出现任何其他文字」在首尾帧之间是会变的。**
   首帧没有文字、尾帧才有，所以禁止项那一句必须跟着改。这与「尾帧只改主体状态」有冲突，
   实际是**两处**要改。已在 A04/A05/A10 的尾帧块里显式标注了「禁止项末句替换为」。

3. **需求方写「动画演示」时，画风轨道不能自己按内容性质推。** 我第一轮把露天雨淋归到写实摄影轨道，
   是没读需求方那四个字。**画风轨道的第一依据是需求方原话，其次才是内容性质。**
   建议补进 `shot-planning.md` 的画风轨道判断依据。

4. **内部结构镜是全片风险最高的一类，而 skill 里没有对应的闸门。**
   A02-A05 四镜的继电器脚数、主板分区、芯片引脚全是按通用工业常识写的，
   没有实物依据。`physical-realism.md` 第 4 节列了「引脚数不要随机」这类要求，
   但没有一条说「没有实物资料时应当主动索要或标注为假设」。
   **建议在 SKILL.md 的确认单里加一项「内部结构镜的资料来源」。**

**未验证**：全部 prompt 尚未出图，`model-routing.md` 的能力表也仍然全部来自文档。
出图后回来记：十三要素的长度模型吃不吃得下、首帧「现象开始之前」的措辞视频模型认不认、
尾帧挂首帧后一致性能保到什么程度。

---

## 2026-08-28（三）· 拿到官方文档，自我推翻两条

**来源**：Kling《VIDEO 3.0》《VIDEO 3.0 Omni》官方指南（浏览器读，WebFetch 被 446 挡）+
**即梦官方《Seedance 2.5 使用手册》**（`docs/Seedance 2.5 使用手册.txt`）。
上一条记录里"待补"的 Seedance 官方文档，这次拿到了。

**推翻了昨天刚写进去的两条**，都是因为昨天只有社区 skill 的存档、把社区约定当成了平台限制：

1. **"Seedance 弃用首尾帧" —— 错。** 官方手册明写「首帧、首尾帧、多模态参考均支持」，
   且首尾帧是界面四大模式之一（全能参考 / 智能编辑 / 超长视频 / 首尾帧）。
   社区那个 skill 的禁用清单是**它自己的路线选择**。
   → 教训：`model-routing.md` 里社区来源和官方来源现在分开标注，冲突时以官方为准。

2. **"禁止让模型做转场" —— 错了一半。** Seedance 2.5 官方手册专门给了转场公式
   `切镜设定 = [转场类型指引] + [基础约束要求] + [切镜逻辑要求]`，
   并列出自然镜头切换 / 淡入淡出 / 叠化 / 白闪·黑闪 / 擦除 / 遮罩转场六种。能力是有的。
   → 改成条件式（`video-prompt.md` 第 8 节）：**段内镜与镜之间让模型做，交付片段的首尾不要自带**。
   后半条的理由也从"模型做不好"换成了真正的理由——**片段自带黑场会锁死剪辑点**。
   并且明确这是默认不是铁律，需求方坚持就照做、但要在镜头表里写明。

**时长约束修正**：Seedance 2.5 单次 **4–30 秒**（不是昨天记的 1–10s，那是界面某个默认档），
超长模式一次 30–180 秒，视频延长可套娃至 60 秒。Kling 3.0/Omni 仍是 3–15 秒。

**新接进来的东西**：

- **Kling 的 `Shot N (Ns):` 语法带秒数**。昨天照社区示例写的是 `Shot 1, …` 不带时长，
  官方示例是 `Shot 1 (2s): …`。逐镜写死时长，别让模型自己均分。
- **`@` 引用语法**。Kling 用 `@元素名` `@Image`；Seedance 用 `@图1` `@图2` 且**官方说全文可多次 @ 会更准**。
  比在正文里再描述一遍外观稳得多，已写进 `image-prompt.md` 的产品参考句一节。
- **Seedance 三模块 prompt 结构**（多模态参考层 / 全局设定 / 时间戳剧本分镜）。
  与本 skill 的十三要素不冲突——**十三要素管"写什么"，三模块管"怎么排"**，走 Seedance 时按三模块摆。
- **非人物主体造型公式**：`[数量词]+[颜色]+[外形/设计风格]+[具体物品名词]+[材质细节/磨损状态]+[标志性特征]`。
  正好适用充电桩，已写进 `image-prompt.md`。它和产品参考句互补：公式管"说清楚"，参考句管"别改"。
- **禁止项的四个方向**：`[声音]+[字幕]+[行为]+[容易崩坏的地方]`。
- **Kling 3.0 Omni** 的定位：多元素融合与一致性更强（多主体同框各自锁特征、视频元素、音色绑定）。
  我们只有一个产品主体、无角色对白，**3.0 够用**，除非同镜要锁 3 个以上参考主体。
- Seedance 2.5 另有绿幕编辑、白模控制（Maya/Blender 插件导 Camera 路线）、局部消除、
  多宫格分镜（官方推荐简约线稿/火柴人）。都记进了 `model-routing.md` 第 3 节末尾，暂未用上。

**分流规则重写**：两边现在都支持首尾帧和多镜头，所以分流看的是**强项和成本**，不是有无。
默认仍走 Kling 3.0（产品静物 + 画面内文字准 + 单镜都在 15 秒内）；
超过 15 秒的连续演进、要绿幕/白模/局部改的走 Seedance 2.5。

**状态**：仍**未实跑验证**。`model-routing.md` 全部来自文档，没有一条是实测的。

---

## 2026-08-28（二）· 接入 Kling 3.0 与即梦资料，修正三处方案性错误

**来源**：Kling 官方《VIDEO 3.0 Model User Guide》（发布 2026-02-06，本日读取，WebFetch 被 446 挡下，
改用浏览器读到）+ `docs/` 下四份即梦技能市场页面存档。
⚠️ **Seedance 官方 prompt 文档没读到** —— 飞书 wiki 302 跳登录，需账号。已记在 `model-routing.md` 第 4 节待补。

**新增 `references/model-routing.md`** —— 唯一存放"会过期的事实"的地方。
其余 references 只写不随版本变的方法论；模型能力对不上就改这一份，不要改别处。

**三处被外部资料推翻的原方案**：

1. **1–2 秒的分镜不能一镜一次生成。** 我原来按台词字数切出了 1.1–3.1 秒的镜头。
   Kling 下限 3s、即梦社区 SOP 下限 4s（且明写"短镜须并入相邻镜头凑够 4s"）。取严的 4 秒。
   → 新增 `frame-strategy.md` 第 10 节，给了三条出路。

2. **连续段应当用 Custom Multi-Shot 一次生成，而不是逐镜生成再拼。**
   Kling 3.0 新增多镜头能力，转场由模型内部完成。这同时解决了上一条的时长问题。
   → 新增 `video-prompt.md` 第 7 节，含官方 `Shot 1, … Shot 2, …` 的语法示例。

3. **"画面瞬间暗下"这类要求不该交给生成模型。** 即梦头部 skill 把黑场/白场/渐显/渐隐/叠化
   列为硬禁令——它们是剪辑动作不是画面内容，让模型做既脏又占秒数。
   → 新增 `video-prompt.md` 第 8 节：生成到暗下之前为止，黑场交给剪辑，且必须在镜头表里写明，
   否则剪辑侧会以为片段自带黑场。

**其余补充**：

- **提交正文必须是纯散文**（`video-prompt.md` 第 4 节从"可以摊成散文"升级为硬约束）。
  不得注入 Markdown 符号、`【】`标签、比例/分辨率等配置项、确认单内容。
  即梦两个头部 skill（合计 2.4 万人在用）措辞几乎一致地把这条写成禁令，与我们本来担心的
  "渲染污染"同源，但更具体。
- **节奏改用显式时间锚点**（`在第 4 秒，…`）。Kling 官方 15 秒示例就是这么写的。
  光写"先慢后快"，模型的"快"和你的"快"不是一回事。
- **链式首尾帧**（`frame-strategy.md` 第 9 节）—— 连续段里上一镜的尾帧图直接用作下一镜首帧图，
  N 镜只需 N+1 张图且零跳变。中间那些图既是尾帧又是首帧，两个身份都要成立。
- **产品参考锁的是六项**（主体识别度/色彩气质/材质逻辑/光影逻辑/构图记忆点/情绪基调），
  锚定 ≠ 复刻原图构图。参考图多于一张时走 Element Reference / 全能参考的多图绑定。
- **视频生成一律关原生音频** —— 配音走另外流程，开着白花 30% 左右。
- 确认单 七项 → **九项**，新增「连续性归属」和「目标视频模型」。
- **画面比例每次都要问**，不许沿用上次（操作者明确要求）。

**本次对齐的其他决定**：
- 本 skill 只交付 **prompt 或图片**，视频制作是别的流程。
- 视频A 的四个画面是成片不同时间点的独立画面，**不考虑连贯性**；
  视频B 的六个分镜是约 10 秒的连续段，**要考虑连续性和转场**。
  这个区别催生了「连续性归属」字段——需求方一段文案里两者会混在一起。

**状态**：仍**未实跑验证**。model-routing.md 的能力表来自文档与页面存档，没有实测。

---

## 2026-08-28 · 建仓，skill 框架落地

**做了什么**：从零建 `video-skill/`，产出 `product-video` skill 的全部文档。
覆盖：需求确认单 → 拆镜 → 首尾帧图像 prompt → 视频 prompt，Claude 侧到 prompt 为止，Codex 侧到出图。

**几个定下来的口径（都是和操作者对齐后的结果，不是我自己推的）**：

- **首帧的语义是「现象开始之前的定格」**，不是现象进行中的截图。这是整套 workflow 最容易做错的一条，
  `frame-strategy.md` 第 2 节用六组正反例钉死。
- **尾帧 prompt 不重写，只做增量** —— 整段复制首帧，只替换变化的那几句。理由不是省事：
  首尾帧的主要失败模式是「除了该变的之外还变了」（机位飘、光位偏、产品细节不一致），
  接成视频就是生硬跳变。配套要求生成尾帧时把首帧图一起挂载。
- **运镜不写进视频 prompt（首尾帧镜头）** —— 首尾帧已经钉死起止构图，再写运镜是两套矛盾指令。
  让模型通过构图差值自洽。**只出首帧的镜头是唯一例外**，那时必须写运镜和终点描述。
- **一个镜头只能有一个变化量。** 需求方常把两个现象写进一句画面说明（如「桩体被雨淋被灰尘吹」），
  必须拆镜，并在确认单里说明为什么拆。
- **产品参考图选哪张是操作者的事**，prompt 内不指定文件，只在产品名词后紧跟固定的括号约束句。
- **不做合规检查** —— 人工审核环节负责。（初版评估时曾计划写 `compliance.md`，
  已按操作者明确要求去掉，不要再加回来。）
- **模型与版本不写进正文** —— 操作者自行准备。写死必然过期且过期了没人发现。
- **Codex 不做出图自审核** —— 生成即可。自审在这条链路上是负收益：基于对自己输出的猜测反复重出，
  烧钱且可能把对的改坏。有疑虑写进交付说明。

**明确没有做的**：
- 没有参考过往视频作品。操作者要求从零开始，不被既有风格束缚。
- 没有建 `styles.registry.json` 那样的机读注册表。四条画风轨道直接写在
  `image-prompt.md` 第 3 节——没有代码消费它，JSON 化是纯负担。

**状态**：全部**未实跑验证**。文档写完，尚未用真实需求跑过一轮。

**下一步**：用视频部门的这份文案实跑一轮（磐石Pro：继电器/主控芯片参数 → 防尘防水 →
无感启充 → 三路监测断电）。实跑中重点验证四件事，验完回来补进本文件：

1. 十三要素写出来的 prompt，图像模型是否真的吃得下（会不会太长导致后半段被忽略）
2. 首帧「现象开始之前」的措辞，视频模型是否真的会从那个状态起步
3. 尾帧挂载首帧后，一致性到底能保到什么程度
4. `physical-realism.md` 的清单有没有漏掉高频穿帮点 —— 这份清单是初版，
   按操作者的意思在应用中持续完善
